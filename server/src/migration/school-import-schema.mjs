export async function ensureSchoolImportSchema(database){await database.query(`
 CREATE TABLE IF NOT EXISTS school_import_jobs(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,import_type text NOT NULL,file_name text,object_key text,mapping_json text NOT NULL DEFAULT '{}',options_json text NOT NULL DEFAULT '{}',status text NOT NULL DEFAULT 'pending' CHECK(status IN('pending','validating','ready','processing','completed','failed','cancelled')),total_rows integer NOT NULL DEFAULT 0,valid_rows integer NOT NULL DEFAULT 0,invalid_rows integer NOT NULL DEFAULT 0,processed_rows integer NOT NULL DEFAULT 0,errors_json text NOT NULL DEFAULT '[]',created_by text REFERENCES users(id) ON DELETE SET NULL,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
 CREATE INDEX IF NOT EXISTS school_import_jobs_org_status_idx ON school_import_jobs(organization_id,import_type,status,created_at DESC);
 `);}
