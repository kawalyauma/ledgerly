export async function ensureSchoolFeeReferenceSchema(database){await database.query(`
 CREATE TABLE IF NOT EXISTS school_fee_accounting_settings(organization_id text PRIMARY KEY REFERENCES organizations(id) ON DELETE CASCADE,receivable_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,discount_account_id text REFERENCES accounts(id) ON DELETE SET NULL,scholarship_account_id text REFERENCES accounts(id) ON DELETE SET NULL,writeoff_account_id text REFERENCES accounts(id) ON DELETE SET NULL,late_fee_income_account_id text REFERENCES accounts(id) ON DELETE SET NULL,default_currency text NOT NULL DEFAULT 'UGX',invoice_due_days integer NOT NULL DEFAULT 30,auto_post_invoices boolean NOT NULL DEFAULT true,auto_post_receipts boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
 CREATE TABLE IF NOT EXISTS school_fee_structures(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,academic_year_id text NOT NULL REFERENCES school_academic_years(id) ON DELETE RESTRICT,term_id text REFERENCES school_terms(id) ON DELETE RESTRICT,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,class_id text REFERENCES school_classes(id) ON DELETE SET NULL,stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,residency_status text,student_category text,currency text NOT NULL DEFAULT 'UGX',effective_from date,effective_to date,priority integer NOT NULL DEFAULT 100,status text NOT NULL DEFAULT 'draft',notes text,metadata_json text NOT NULL DEFAULT '{}',created_by text REFERENCES users(id) ON DELETE SET NULL,updated_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
 CREATE INDEX IF NOT EXISTS school_fee_structures_match_idx ON school_fee_structures(organization_id,academic_year_id,term_id,campus_id,class_level_id,class_id,stream_id,status,priority);
 CREATE TABLE IF NOT EXISTS school_fee_structure_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,structure_id text NOT NULL REFERENCES school_fee_structures(id) ON DELETE CASCADE,fee_category_id text NOT NULL REFERENCES school_fee_categories(id) ON DELETE RESTRICT,amount_minor bigint NOT NULL CHECK(amount_minor>=0),quantity_micros bigint NOT NULL DEFAULT 1000000 CHECK(quantity_micros>0),due_date date,mandatory boolean NOT NULL DEFAULT true,discountable boolean NOT NULL DEFAULT true,installment_allowed boolean NOT NULL DEFAULT true,refundable boolean NOT NULL DEFAULT false,tax_minor bigint NOT NULL DEFAULT 0 CHECK(tax_minor>=0),tax_account_id text REFERENCES accounts(id) ON DELETE SET NULL,description text,metadata_json text NOT NULL DEFAULT '{}',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,structure_id,fee_category_id));
 CREATE INDEX IF NOT EXISTS school_fee_structure_lines_structure_idx ON school_fee_structure_lines(organization_id,structure_id);
 CREATE TABLE IF NOT EXISTS school_fee_discount_schemes(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,category text NOT NULL DEFAULT 'discount',calculation_type text NOT NULL,amount_minor bigint,rate_micros bigint,fee_category_id text REFERENCES school_fee_categories(id) ON DELETE SET NULL,academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,term_id text REFERENCES school_terms(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,class_id text REFERENCES school_classes(id) ON DELETE SET NULL,residency_status text,student_category text,sibling_min_count integer,max_amount_minor bigint,priority integer NOT NULL DEFAULT 100,stackable boolean NOT NULL DEFAULT false,approval_required boolean NOT NULL DEFAULT false,accounting_treatment text NOT NULL DEFAULT 'net_revenue',expense_account_id text REFERENCES accounts(id) ON DELETE SET NULL,starts_on date,ends_on date,active boolean NOT NULL DEFAULT true,metadata_json text NOT NULL DEFAULT '{}',created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code),CHECK((calculation_type='fixed' AND amount_minor IS NOT NULL AND amount_minor>=0) OR (calculation_type='percentage' AND rate_micros IS NOT NULL AND rate_micros>=0 AND rate_micros<=100000000)));
 CREATE INDEX IF NOT EXISTS school_fee_discount_schemes_match_idx ON school_fee_discount_schemes(organization_id,active,academic_year_id,term_id,fee_category_id,priority);
 `);}
