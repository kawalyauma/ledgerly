export async function ensureSchoolConfigurationSchema(database) {
  await database.query(`
    ALTER TABLE school_profiles ADD COLUMN IF NOT EXISTS logo_file_id text;
    ALTER TABLE school_user_profiles ADD COLUMN IF NOT EXISTS profile_photo_file_id text;
    ALTER TABLE school_user_profiles ADD COLUMN IF NOT EXISTS signature_file_id text;

    CREATE TABLE IF NOT EXISTS school_files (
      id text PRIMARY KEY, organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
      object_key text NOT NULL, original_name text NOT NULL, mime_type text NOT NULL, size_bytes bigint NOT NULL,
      checksum_sha256 text NOT NULL, purpose text NOT NULL DEFAULT 'document', uploaded_by text REFERENCES users(id) ON DELETE SET NULL,
      deleted_at timestamptz, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now(),
      UNIQUE(organization_id,object_key)
    );
    CREATE INDEX IF NOT EXISTS school_files_org_created_idx ON school_files(organization_id,created_at DESC);

    CREATE TABLE IF NOT EXISTS school_grading_scales (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,curriculum text,is_default boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE UNIQUE INDEX IF NOT EXISTS school_grading_default_uq ON school_grading_scales(organization_id) WHERE is_default AND active;
    CREATE TABLE IF NOT EXISTS school_grade_boundaries (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,grading_scale_id text NOT NULL REFERENCES school_grading_scales(id) ON DELETE CASCADE,grade text NOT NULL,min_score double precision NOT NULL,max_score double precision NOT NULL,points double precision,aggregate_points integer,remark text,color_hex text,sequence_no integer NOT NULL DEFAULT 0,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),CHECK(min_score<=max_score),UNIQUE(organization_id,grading_scale_id,grade));
    CREATE TABLE IF NOT EXISTS school_divisions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,grading_scale_id text REFERENCES school_grading_scales(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,min_aggregate integer,max_aggregate integer,min_subjects integer,rule_json text NOT NULL DEFAULT '{}',sequence_no integer NOT NULL DEFAULT 0,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_assessment_types (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,weight_percent double precision NOT NULL DEFAULT 100,max_score double precision NOT NULL DEFAULT 100,sequence_no integer NOT NULL DEFAULT 0,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_promotion_rules (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,class_level_id text REFERENCES school_class_levels(id) ON DELETE CASCADE,name text NOT NULL,minimum_average double precision,maximum_failed_subjects integer,minimum_attendance_percent double precision,target_class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,allow_manual_override boolean NOT NULL DEFAULT true,rule_json text NOT NULL DEFAULT '{}',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_promotion_rules_org_level_idx ON school_promotion_rules(organization_id,class_level_id,active);
    CREATE TABLE IF NOT EXISTS school_calendar_events (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,campus_id text REFERENCES school_branches(id) ON DELETE CASCADE,academic_year_id text REFERENCES school_academic_years(id) ON DELETE CASCADE,term_id text REFERENCES school_terms(id) ON DELETE CASCADE,event_type text NOT NULL,title text NOT NULL,description text,starts_at timestamptz NOT NULL,ends_at timestamptz NOT NULL,all_day boolean NOT NULL DEFAULT true,teaching_day boolean NOT NULL DEFAULT false,recurrence_rule text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),CHECK(starts_at<=ends_at));
    CREATE INDEX IF NOT EXISTS school_calendar_range_idx ON school_calendar_events(organization_id,starts_at,ends_at);
    CREATE TABLE IF NOT EXISTS school_fee_categories (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,description text,income_account_id text REFERENCES accounts(id) ON DELETE SET NULL,receivable_account_id text REFERENCES accounts(id) ON DELETE SET NULL,product_id text,taxable boolean NOT NULL DEFAULT false,tax_code text,refundable boolean NOT NULL DEFAULT false,mandatory boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,metadata_json text NOT NULL DEFAULT '{}',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_payment_methods (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,method_type text NOT NULL,account_id text REFERENCES accounts(id) ON DELETE SET NULL,configuration_json text NOT NULL DEFAULT '{}',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_settings (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,setting_group text NOT NULL,setting_key text NOT NULL,value_json text NOT NULL,updated_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,setting_group,setting_key));
    CREATE INDEX IF NOT EXISTS school_settings_group_idx ON school_settings(organization_id,setting_group);
    CREATE TABLE IF NOT EXISTS school_document_templates (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,template_type text NOT NULL,name text NOT NULL,version integer NOT NULL DEFAULT 1,content_json text NOT NULL DEFAULT '{}',is_default boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),file_id text REFERENCES school_files(id) ON DELETE SET NULL);
    CREATE INDEX IF NOT EXISTS school_templates_type_idx ON school_document_templates(organization_id,template_type,active);
    CREATE TABLE IF NOT EXISTS school_roles (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,description text,role_category text NOT NULL DEFAULT 'custom',system_role boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_role_permissions (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,role_id text NOT NULL REFERENCES school_roles(id) ON DELETE CASCADE,permission text NOT NULL,effect text NOT NULL DEFAULT 'allow' CHECK(effect IN ('allow','deny')),created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,role_id,permission));
    CREATE TABLE IF NOT EXISTS school_user_roles (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,user_id text NOT NULL REFERENCES users(id) ON DELETE CASCADE,role_id text NOT NULL REFERENCES school_roles(id) ON DELETE CASCADE,starts_at timestamptz,ends_at timestamptz,assigned_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,user_id,role_id));
    CREATE TABLE IF NOT EXISTS school_user_access (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,user_id text NOT NULL REFERENCES users(id) ON DELETE CASCADE,access_type text NOT NULL,resource_id text NOT NULL,access_level text NOT NULL DEFAULT 'manage',starts_at timestamptz,ends_at timestamptz,assigned_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,user_id,access_type,resource_id,access_level));
    CREATE INDEX IF NOT EXISTS school_user_access_lookup_idx ON school_user_access(organization_id,user_id,access_type,resource_id);
    CREATE TABLE IF NOT EXISTS school_temporary_permissions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,user_id text NOT NULL REFERENCES users(id) ON DELETE CASCADE,permission text NOT NULL,starts_at timestamptz NOT NULL,ends_at timestamptz NOT NULL,reason text,granted_by text REFERENCES users(id) ON DELETE SET NULL,revoked_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),CHECK(starts_at<ends_at));
    CREATE TABLE IF NOT EXISTS school_security_policies (organization_id text PRIMARY KEY REFERENCES organizations(id) ON DELETE CASCADE,minimum_password_length integer NOT NULL DEFAULT 12,require_uppercase boolean NOT NULL DEFAULT true,require_lowercase boolean NOT NULL DEFAULT true,require_number boolean NOT NULL DEFAULT true,require_symbol boolean NOT NULL DEFAULT false,password_expiry_days integer,password_history_count integer NOT NULL DEFAULT 5,max_failed_logins integer NOT NULL DEFAULT 5,lockout_minutes integer NOT NULL DEFAULT 15,session_timeout_minutes integer NOT NULL DEFAULT 30,require_2fa_for_admins boolean NOT NULL DEFAULT false,allow_impersonation boolean NOT NULL DEFAULT false,updated_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_impersonation_sessions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,administrator_user_id text NOT NULL REFERENCES users(id) ON DELETE CASCADE,target_user_id text NOT NULL REFERENCES users(id) ON DELETE CASCADE,reason text NOT NULL,starts_at timestamptz NOT NULL DEFAULT now(),ends_at timestamptz,ip_address text,created_at timestamptz NOT NULL DEFAULT now());
  `);
}

export async function finalizeSchoolConfigurationSchema(database) {
  await database.query(`
    DO $$ BEGIN
      IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_profiles_logo_file_fk') THEN ALTER TABLE school_profiles ADD CONSTRAINT school_profiles_logo_file_fk FOREIGN KEY(logo_file_id) REFERENCES school_files(id) ON DELETE SET NULL; END IF;
      IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_user_profiles_photo_file_fk') THEN ALTER TABLE school_user_profiles ADD CONSTRAINT school_user_profiles_photo_file_fk FOREIGN KEY(profile_photo_file_id) REFERENCES school_files(id) ON DELETE SET NULL; END IF;
      IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_user_profiles_signature_file_fk') THEN ALTER TABLE school_user_profiles ADD CONSTRAINT school_user_profiles_signature_file_fk FOREIGN KEY(signature_file_id) REFERENCES school_files(id) ON DELETE SET NULL; END IF;
      IF to_regclass('public.products') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_fee_categories_product_fk') THEN ALTER TABLE school_fee_categories ADD CONSTRAINT school_fee_categories_product_fk FOREIGN KEY(product_id) REFERENCES products(id) ON DELETE SET NULL; END IF;
    END $$;
  `);
}

export async function ensureSchoolPeopleSchema(database) {
  await database.query(`
    CREATE TABLE IF NOT EXISTS school_admission_applications (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,application_number text NOT NULL,status text NOT NULL DEFAULT 'submitted',desired_class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,applicant_json text NOT NULL DEFAULT '{}',guardian_json text NOT NULL DEFAULT '{}',screening_json text NOT NULL DEFAULT '{}',decision_notes text,submitted_at timestamptz,decided_at timestamptz,decided_by text REFERENCES users(id) ON DELETE SET NULL,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,application_number));
    CREATE INDEX IF NOT EXISTS school_admissions_status_idx ON school_admission_applications(organization_id,status,academic_year_id);
    CREATE TABLE IF NOT EXISTS school_students (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text,user_id text REFERENCES users(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,admission_application_id text REFERENCES school_admission_applications(id) ON DELETE SET NULL,admission_number text NOT NULL,student_number text NOT NULL,first_name text NOT NULL,middle_name text,last_name text NOT NULL,preferred_name text,gender text,date_of_birth date,nationality text,place_of_birth text,religion text,home_language text,phone text,email text,physical_address text,previous_school text,previous_class text,admission_date date NOT NULL,admission_class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,current_academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,current_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,current_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,student_category text,residency_status text NOT NULL DEFAULT 'day',house text,status text NOT NULL DEFAULT 'active',financial_sponsor_contact_id text,profile_photo_url text,barcode_value text,qr_code_value text,tags_json text NOT NULL DEFAULT '[]',custom_fields_json text NOT NULL DEFAULT '{}',created_by text REFERENCES users(id) ON DELETE SET NULL,updated_by text REFERENCES users(id) ON DELETE SET NULL,deleted_by text REFERENCES users(id) ON DELETE SET NULL,deleted_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),profile_photo_file_id text REFERENCES school_files(id) ON DELETE SET NULL,UNIQUE(organization_id,admission_number),UNIQUE(organization_id,student_number));
    CREATE INDEX IF NOT EXISTS school_students_name_idx ON school_students(organization_id,last_name,first_name);
    CREATE INDEX IF NOT EXISTS school_students_class_idx ON school_students(organization_id,current_academic_year_id,current_class_id,current_stream_id,status);
    CREATE INDEX IF NOT EXISTS school_students_status_date_idx ON school_students(organization_id,status,admission_date);
    CREATE TABLE IF NOT EXISTS school_guardians (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text,user_id text REFERENCES users(id) ON DELETE SET NULL,first_name text NOT NULL,middle_name text,last_name text NOT NULL,phone_primary text,phone_secondary text,email text,relationship_default text,occupation text,employer text,physical_address text,national_id text,profile_photo_url text,notification_preferences_json text NOT NULL DEFAULT '{}',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),profile_photo_file_id text REFERENCES school_files(id) ON DELETE SET NULL);
    CREATE INDEX IF NOT EXISTS school_guardians_contact_idx ON school_guardians(organization_id,phone_primary,email);
    CREATE TABLE IF NOT EXISTS school_student_guardians (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,guardian_id text NOT NULL REFERENCES school_guardians(id) ON DELETE CASCADE,relationship text NOT NULL,is_primary boolean NOT NULL DEFAULT false,is_emergency_contact boolean NOT NULL DEFAULT false,is_authorized_pickup boolean NOT NULL DEFAULT false,is_financially_responsible boolean NOT NULL DEFAULT false,receives_academic_updates boolean NOT NULL DEFAULT true,receives_financial_updates boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,student_id,guardian_id));
    CREATE UNIQUE INDEX IF NOT EXISTS school_student_primary_guardian_uq ON school_student_guardians(organization_id,student_id) WHERE is_primary;
    CREATE TABLE IF NOT EXISTS school_authorized_pickups (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,name text NOT NULL,relationship text,phone text,national_id text,photo_url text,starts_on date,ends_on date,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),photo_file_id text REFERENCES school_files(id) ON DELETE SET NULL);
    CREATE TABLE IF NOT EXISTS school_student_medical (student_id text PRIMARY KEY REFERENCES school_students(id) ON DELETE CASCADE,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,blood_group text,allergies_json text NOT NULL DEFAULT '[]',conditions_json text NOT NULL DEFAULT '[]',medications_json text NOT NULL DEFAULT '[]',disabilities_json text NOT NULL DEFAULT '[]',special_education_needs_json text NOT NULL DEFAULT '[]',doctor_name text,doctor_phone text,insurance_json text NOT NULL DEFAULT '{}',notes text,updated_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_enrollments (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,academic_year_id text NOT NULL REFERENCES school_academic_years(id) ON DELETE RESTRICT,class_id text NOT NULL REFERENCES school_classes(id) ON DELETE RESTRICT,stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,enrolled_on date NOT NULL,left_on date,status text NOT NULL DEFAULT 'active',reason text,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,student_id,academic_year_id));
    CREATE INDEX IF NOT EXISTS school_enrollment_class_idx ON school_enrollments(organization_id,academic_year_id,class_id,stream_id,status);
    CREATE TABLE IF NOT EXISTS school_student_status_history (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,from_status text,to_status text NOT NULL,reason text,effective_on date NOT NULL,changed_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_student_status_history_idx ON school_student_status_history(organization_id,student_id,effective_on DESC);
    CREATE TABLE IF NOT EXISTS school_student_promotions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,from_academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,to_academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,from_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,to_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,from_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,to_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,decision text NOT NULL,reason text,rule_snapshot_json text NOT NULL DEFAULT '{}',effective_on date NOT NULL,approved_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_student_promotions_idx ON school_student_promotions(organization_id,student_id,effective_on DESC);
    CREATE TABLE IF NOT EXISTS school_student_transfers (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,transfer_type text NOT NULL,from_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,to_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,from_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,to_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,from_campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,to_campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,destination_school text,reason text,effective_on date NOT NULL,document_template_id text REFERENCES school_document_templates(id) ON DELETE SET NULL,approved_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_student_documents (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text REFERENCES school_students(id) ON DELETE CASCADE,admission_application_id text REFERENCES school_admission_applications(id) ON DELETE CASCADE,document_type text NOT NULL,name text NOT NULL,object_key text NOT NULL,mime_type text,size_bytes bigint,checksum text,issued_on date,expires_on date,verification_status text NOT NULL DEFAULT 'unverified',verified_by text REFERENCES users(id) ON DELETE SET NULL,uploaded_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),file_id text REFERENCES school_files(id) ON DELETE SET NULL);
    CREATE INDEX IF NOT EXISTS school_student_docs_idx ON school_student_documents(organization_id,student_id,document_type);
    CREATE TABLE IF NOT EXISTS school_student_notes (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,note_type text NOT NULL DEFAULT 'general',body text NOT NULL,confidential boolean NOT NULL DEFAULT false,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_student_tags (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,color_hex text,active boolean NOT NULL DEFAULT true,UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_student_tag_links (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,tag_id text NOT NULL REFERENCES school_student_tags(id) ON DELETE CASCADE,PRIMARY KEY(organization_id,student_id,tag_id));
    CREATE TABLE IF NOT EXISTS school_student_siblings (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,sibling_student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,student_id,sibling_student_id),CHECK(student_id<>sibling_student_id));
    CREATE TABLE IF NOT EXISTS school_student_timeline (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,event_type text NOT NULL,title text NOT NULL,description text,source_type text,source_id text,event_at timestamptz NOT NULL,actor_id text REFERENCES users(id) ON DELETE SET NULL,metadata_json text NOT NULL DEFAULT '{}',created_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_student_timeline_idx ON school_student_timeline(organization_id,student_id,event_at DESC);
    CREATE TABLE IF NOT EXISTS school_promotion_runs (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,from_academic_year_id text NOT NULL REFERENCES school_academic_years(id) ON DELETE RESTRICT,to_academic_year_id text NOT NULL REFERENCES school_academic_years(id) ON DELETE RESTRICT,source_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,source_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,effective_on date NOT NULL,status text NOT NULL DEFAULT 'draft',notes text,total_students integer NOT NULL DEFAULT 0,recommended_promotions integer NOT NULL DEFAULT 0,recommended_repeats integer NOT NULL DEFAULT 0,recommended_graduations integer NOT NULL DEFAULT 0,review_required boolean NOT NULL DEFAULT false,applied_students integer NOT NULL DEFAULT 0,created_by text REFERENCES users(id) ON DELETE SET NULL,applied_by text REFERENCES users(id) ON DELETE SET NULL,applied_at timestamptz,cancelled_by text REFERENCES users(id) ON DELETE SET NULL,cancelled_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),CHECK(from_academic_year_id<>to_academic_year_id));
    CREATE INDEX IF NOT EXISTS school_promotion_runs_org_idx ON school_promotion_runs(organization_id,from_academic_year_id,to_academic_year_id,created_at DESC);
    CREATE TABLE IF NOT EXISTS school_promotion_run_items (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,run_id text NOT NULL REFERENCES school_promotion_runs(id) ON DELETE CASCADE,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,from_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,from_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,source_class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,target_class_level_id text REFERENCES school_class_levels(id) ON DELETE SET NULL,target_class_id text REFERENCES school_classes(id) ON DELETE SET NULL,target_stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,average_percent double precision,failed_subjects integer,attendance_percent double precision,metrics_json text NOT NULL DEFAULT '{}',rule_snapshot_json text NOT NULL DEFAULT '{}',recommended_decision text NOT NULL,recommendation_reason text,final_decision text,override_reason text,item_status text NOT NULL DEFAULT 'pending',applied_promotion_id text REFERENCES school_student_promotions(id) ON DELETE SET NULL,error_message text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,run_id,student_id));
    CREATE INDEX IF NOT EXISTS school_promotion_items_student_idx ON school_promotion_run_items(organization_id,student_id,created_at DESC);
    CREATE TABLE IF NOT EXISTS school_discipline_offence_types (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,category text,default_severity text NOT NULL DEFAULT 'medium',default_points integer NOT NULL DEFAULT 0,default_action text,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE TABLE IF NOT EXISTS school_discipline_incidents (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,incident_number text NOT NULL,student_id text NOT NULL REFERENCES school_students(id) ON DELETE CASCADE,academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,term_id text REFERENCES school_terms(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,offence_type_id text REFERENCES school_discipline_offence_types(id) ON DELETE SET NULL,record_type text NOT NULL DEFAULT 'incident',title text NOT NULL,description text NOT NULL,severity text NOT NULL DEFAULT 'medium',points integer NOT NULL DEFAULT 0,incident_at timestamptz NOT NULL,location text,witness_notes text,status text NOT NULL DEFAULT 'open',follow_up_on date,confidential boolean NOT NULL DEFAULT false,reported_by text REFERENCES users(id) ON DELETE SET NULL,assigned_to text REFERENCES users(id) ON DELETE SET NULL,resolved_by text REFERENCES users(id) ON DELETE SET NULL,resolved_at timestamptz,resolution_notes text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,incident_number));
    CREATE INDEX IF NOT EXISTS school_discipline_incident_student_idx ON school_discipline_incidents(organization_id,student_id,incident_at DESC);
    CREATE INDEX IF NOT EXISTS school_discipline_incident_status_idx ON school_discipline_incidents(organization_id,status,follow_up_on,incident_at DESC);
    CREATE TABLE IF NOT EXISTS school_discipline_actions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,incident_id text NOT NULL REFERENCES school_discipline_incidents(id) ON DELETE CASCADE,action_type text NOT NULL,title text NOT NULL,notes text,starts_on date,ends_on date,status text NOT NULL DEFAULT 'planned',issued_by text REFERENCES users(id) ON DELETE SET NULL,completed_by text REFERENCES users(id) ON DELETE SET NULL,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_discipline_parent_meetings (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,incident_id text NOT NULL REFERENCES school_discipline_incidents(id) ON DELETE CASCADE,guardian_id text REFERENCES school_guardians(id) ON DELETE SET NULL,scheduled_at timestamptz NOT NULL,held_at timestamptz,status text NOT NULL DEFAULT 'scheduled',attendees text,agenda text,minutes text,outcome text,recorded_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE TABLE IF NOT EXISTS school_discipline_followups (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,incident_id text NOT NULL REFERENCES school_discipline_incidents(id) ON DELETE CASCADE,due_on date NOT NULL,note text NOT NULL,status text NOT NULL DEFAULT 'pending',completed_note text,created_by text REFERENCES users(id) ON DELETE SET NULL,completed_by text REFERENCES users(id) ON DELETE SET NULL,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_discipline_followup_due_idx ON school_discipline_followups(organization_id,status,due_on);
    CREATE TABLE IF NOT EXISTS school_discipline_attachments (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,incident_id text NOT NULL REFERENCES school_discipline_incidents(id) ON DELETE CASCADE,file_id text NOT NULL REFERENCES school_files(id) ON DELETE RESTRICT,attachment_type text NOT NULL DEFAULT 'evidence',caption text,uploaded_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,incident_id,file_id));
  `);
}

export async function finalizeSchoolPeopleSchema(database) {
  await database.query(`
    DO $$ BEGIN
      IF to_regclass('public.contacts') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_students_contact_fk') THEN ALTER TABLE school_students ADD CONSTRAINT school_students_contact_fk FOREIGN KEY(contact_id) REFERENCES contacts(id) ON DELETE SET NULL; END IF;
      IF to_regclass('public.contacts') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_students_sponsor_contact_fk') THEN ALTER TABLE school_students ADD CONSTRAINT school_students_sponsor_contact_fk FOREIGN KEY(financial_sponsor_contact_id) REFERENCES contacts(id) ON DELETE SET NULL; END IF;
      IF to_regclass('public.contacts') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_guardians_contact_fk') THEN ALTER TABLE school_guardians ADD CONSTRAINT school_guardians_contact_fk FOREIGN KEY(contact_id) REFERENCES contacts(id) ON DELETE SET NULL; END IF;
    END $$;
  `);
}

export async function ensureSchoolStaffSchema(database) {
  await database.query(`
    CREATE TABLE IF NOT EXISTS school_staff_positions (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,department_id text REFERENCES school_departments(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,code text NOT NULL,name text NOT NULL,description text,job_grade text,is_teaching boolean NOT NULL DEFAULT false,is_management boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
    CREATE INDEX IF NOT EXISTS school_staff_positions_org_idx ON school_staff_positions(organization_id,department_id,active);
    CREATE TABLE IF NOT EXISTS school_staff_profiles (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text NOT NULL,payroll_employee_id text NOT NULL,user_id text REFERENCES users(id) ON DELETE SET NULL,payable_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,staff_number text NOT NULL,first_name text NOT NULL,middle_name text,last_name text NOT NULL,preferred_name text,gender text,date_of_birth date,nationality text,national_id text,tax_identifier text,phone text,alternate_phone text,email text,physical_address text,postal_address text,department_id text REFERENCES school_departments(id) ON DELETE SET NULL,position_id text REFERENCES school_staff_positions(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,employment_type text NOT NULL DEFAULT 'permanent',employment_status text NOT NULL DEFAULT 'active',is_teacher boolean NOT NULL DEFAULT false,hire_date date NOT NULL,termination_date date,base_pay_minor bigint NOT NULL DEFAULT 0,pay_type text NOT NULL DEFAULT 'salary',currency text NOT NULL DEFAULT 'UGX',profile_photo_file_id text REFERENCES school_files(id) ON DELETE SET NULL,notes text,created_by text REFERENCES users(id) ON DELETE SET NULL,updated_by text REFERENCES users(id) ON DELETE SET NULL,deleted_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,staff_number),UNIQUE(organization_id,contact_id),UNIQUE(organization_id,payroll_employee_id));
    CREATE INDEX IF NOT EXISTS school_staff_org_name_idx ON school_staff_profiles(organization_id,last_name,first_name);
    CREATE INDEX IF NOT EXISTS school_staff_org_assignment_idx ON school_staff_profiles(organization_id,campus_id,department_id,position_id,employment_status);
    CREATE TABLE IF NOT EXISTS school_staff_emergency_contacts (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,staff_id text NOT NULL REFERENCES school_staff_profiles(id) ON DELETE CASCADE,name text NOT NULL,relationship text,phone text NOT NULL,alternate_phone text,email text,physical_address text,is_primary boolean NOT NULL DEFAULT false,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE UNIQUE INDEX IF NOT EXISTS school_staff_primary_emergency_uq ON school_staff_emergency_contacts(organization_id,staff_id) WHERE is_primary;
    CREATE TABLE IF NOT EXISTS school_staff_qualifications (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,staff_id text NOT NULL REFERENCES school_staff_profiles(id) ON DELETE CASCADE,qualification_type text NOT NULL,qualification_name text NOT NULL,institution text,field_of_study text,level text,grade text,awarded_on date,expires_on date,registration_number text,file_id text REFERENCES school_files(id) ON DELETE SET NULL,verified boolean NOT NULL DEFAULT false,verified_by text REFERENCES users(id) ON DELETE SET NULL,verified_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_staff_qualifications_staff_idx ON school_staff_qualifications(organization_id,staff_id);
    CREATE TABLE IF NOT EXISTS school_staff_subjects (organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,staff_id text NOT NULL REFERENCES school_staff_profiles(id) ON DELETE CASCADE,subject_id text NOT NULL REFERENCES school_subjects(id) ON DELETE RESTRICT,is_primary boolean NOT NULL DEFAULT false,proficiency_level text,created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,staff_id,subject_id));
    CREATE TABLE IF NOT EXISTS school_staff_teaching_assignments (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,staff_id text NOT NULL REFERENCES school_staff_profiles(id) ON DELETE CASCADE,academic_year_id text REFERENCES school_academic_years(id) ON DELETE SET NULL,term_id text REFERENCES school_terms(id) ON DELETE SET NULL,campus_id text REFERENCES school_branches(id) ON DELETE SET NULL,class_id text NOT NULL REFERENCES school_classes(id) ON DELETE CASCADE,stream_id text REFERENCES school_streams(id) ON DELETE SET NULL,subject_id text NOT NULL REFERENCES school_subjects(id) ON DELETE RESTRICT,assignment_role text NOT NULL DEFAULT 'teacher',starts_on date,ends_on date,active boolean NOT NULL DEFAULT true,created_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE UNIQUE INDEX IF NOT EXISTS school_staff_teaching_assignment_uq ON school_staff_teaching_assignments(organization_id,staff_id,COALESCE(academic_year_id,''),COALESCE(term_id,''),class_id,COALESCE(stream_id,''),subject_id);
    CREATE INDEX IF NOT EXISTS school_staff_teaching_class_idx ON school_staff_teaching_assignments(organization_id,class_id,stream_id,subject_id,active);
    CREATE TABLE IF NOT EXISTS school_staff_documents (id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,staff_id text NOT NULL REFERENCES school_staff_profiles(id) ON DELETE CASCADE,document_type text NOT NULL,name text NOT NULL,file_id text NOT NULL REFERENCES school_files(id) ON DELETE RESTRICT,issued_on date,expires_on date,notes text,uploaded_by text REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
    CREATE INDEX IF NOT EXISTS school_staff_documents_staff_idx ON school_staff_documents(organization_id,staff_id,document_type);
  `);
}

export async function finalizeSchoolStaffSchema(database) {
  await database.query(`
    DO $$ BEGIN
      IF to_regclass('public.contacts') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_staff_profiles_contact_fk') THEN ALTER TABLE school_staff_profiles ADD CONSTRAINT school_staff_profiles_contact_fk FOREIGN KEY(contact_id) REFERENCES contacts(id) ON DELETE RESTRICT; END IF;
      IF to_regclass('public.employees') IS NOT NULL AND NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname='school_staff_profiles_employee_fk') THEN ALTER TABLE school_staff_profiles ADD CONSTRAINT school_staff_profiles_employee_fk FOREIGN KEY(payroll_employee_id) REFERENCES employees(id) ON DELETE RESTRICT; END IF;
    END $$;
  `);
}
