export async function ensureFinanceOperationsSchema(database){await database.query(`
ALTER TABLE organizations ADD COLUMN IF NOT EXISTS address_json text NOT NULL DEFAULT '{}';
ALTER TABLE organizations ADD COLUMN IF NOT EXISTS tax_registration_number text;
ALTER TABLE organizations ADD COLUMN IF NOT EXISTS document_numbering_json text NOT NULL DEFAULT '{}';
CREATE TABLE IF NOT EXISTS fiscal_periods(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,name text NOT NULL,starts_on date NOT NULL,ends_on date NOT NULL,status text NOT NULL DEFAULT 'open',locked_at timestamptz,locked_by text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,starts_on,ends_on),CHECK(ends_on>=starts_on));
CREATE TABLE IF NOT EXISTS account_groups(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,type text NOT NULL,parent_group_id text REFERENCES account_groups(id) ON DELETE RESTRICT,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
ALTER TABLE accounts ADD COLUMN IF NOT EXISTS parent_account_id text REFERENCES accounts(id) ON DELETE RESTRICT;ALTER TABLE accounts ADD COLUMN IF NOT EXISTS account_group_id text REFERENCES account_groups(id) ON DELETE RESTRICT;
ALTER TABLE contacts ADD COLUMN IF NOT EXISTS credit_limit_minor bigint NOT NULL DEFAULT 0;ALTER TABLE contacts ADD COLUMN IF NOT EXISTS pricing_tier text;ALTER TABLE contacts ADD COLUMN IF NOT EXISTS archived_at timestamptz;
ALTER TABLE products ADD COLUMN IF NOT EXISTS costing_method text NOT NULL DEFAULT 'average';ALTER TABLE products ADD COLUMN IF NOT EXISTS committed_quantity_micros bigint NOT NULL DEFAULT 0;
ALTER TABLE documents ADD COLUMN IF NOT EXISTS approval_status text NOT NULL DEFAULT 'not_required';ALTER TABLE document_lines ADD COLUMN IF NOT EXISTS dimensions_json text NOT NULL DEFAULT '{}';
ALTER TABLE budgets ADD COLUMN IF NOT EXISTS version integer NOT NULL DEFAULT 1;ALTER TABLE budgets ADD COLUMN IF NOT EXISTS scenario text NOT NULL DEFAULT 'base';ALTER TABLE budgets ADD COLUMN IF NOT EXISTS kind text NOT NULL DEFAULT 'annual';ALTER TABLE budgets ADD COLUMN IF NOT EXISTS parent_id text;ALTER TABLE budgets ADD COLUMN IF NOT EXISTS locked_at timestamptz;ALTER TABLE budgets ADD COLUMN IF NOT EXISTS locked_by text;ALTER TABLE budgets ADD COLUMN IF NOT EXISTS submitted_by text;ALTER TABLE budgets ADD COLUMN IF NOT EXISTS approved_by text;
CREATE TABLE IF NOT EXISTS contact_addresses(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text NOT NULL REFERENCES contacts(id) ON DELETE CASCADE,type text NOT NULL,line1 text NOT NULL,line2 text,city text,state text,postal_code text,country text NOT NULL,is_default boolean NOT NULL DEFAULT false,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS contact_people(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text NOT NULL REFERENCES contacts(id) ON DELETE CASCADE,name text NOT NULL,email text,phone text,role text,is_primary boolean NOT NULL DEFAULT false,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS supplier_bank_accounts(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text NOT NULL REFERENCES contacts(id) ON DELETE RESTRICT,bank_name text NOT NULL,account_name text NOT NULL,account_number_masked text NOT NULL,account_number_encrypted text NOT NULL,branch_code text,swift_bic text,currency text NOT NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS tax_jurisdictions(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,country text NOT NULL,authority_name text,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
CREATE TABLE IF NOT EXISTS tax_codes(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,jurisdiction_id text REFERENCES tax_jurisdictions(id) ON DELETE RESTRICT,code text NOT NULL,name text NOT NULL,rate_micros bigint NOT NULL,calculation text NOT NULL DEFAULT 'exclusive',tax_type text NOT NULL,recoverable_percent_micros bigint NOT NULL DEFAULT 1000000,sales_account_id text REFERENCES accounts(id) ON DELETE RESTRICT,purchase_account_id text REFERENCES accounts(id) ON DELETE RESTRICT,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
CREATE TABLE IF NOT EXISTS tax_exemptions(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,contact_id text REFERENCES contacts(id) ON DELETE CASCADE,tax_code_id text REFERENCES tax_codes(id) ON DELETE CASCADE,certificate_number text,reason text NOT NULL,starts_on date,ends_on date,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS tax_returns(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,jurisdiction_id text NOT NULL REFERENCES tax_jurisdictions(id) ON DELETE RESTRICT,period_start date NOT NULL,period_end date NOT NULL,status text NOT NULL DEFAULT 'draft',output_tax_minor bigint NOT NULL DEFAULT 0,input_tax_minor bigint NOT NULL DEFAULT 0,withholding_minor bigint NOT NULL DEFAULT 0,net_tax_minor bigint NOT NULL DEFAULT 0,prepared_by text NOT NULL,locked_at timestamptz,locked_by text,filed_at timestamptz,filing_reference text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS bank_accounts(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,ledger_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,name text NOT NULL,bank_name text,account_number_masked text,currency text NOT NULL,opening_balance_minor bigint NOT NULL DEFAULT 0,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,ledger_account_id));
CREATE TABLE IF NOT EXISTS bank_statement_imports(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,bank_account_id text NOT NULL REFERENCES bank_accounts(id) ON DELETE RESTRICT,filename text NOT NULL,statement_start date,statement_end date,opening_balance_minor bigint,closing_balance_minor bigint,status text NOT NULL DEFAULT 'imported',imported_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS bank_transactions(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,bank_account_id text NOT NULL REFERENCES bank_accounts(id) ON DELETE RESTRICT,import_id text REFERENCES bank_statement_imports(id) ON DELETE SET NULL,external_id text,transaction_date date NOT NULL,description text NOT NULL,reference text,amount_minor bigint NOT NULL,status text NOT NULL DEFAULT 'unmatched',matched_journal_line_id text REFERENCES journal_lines(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE UNIQUE INDEX IF NOT EXISTS bank_transactions_external_uq ON bank_transactions(organization_id,bank_account_id,external_id);
CREATE TABLE IF NOT EXISTS bank_reconciliations(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,bank_account_id text NOT NULL REFERENCES bank_accounts(id) ON DELETE RESTRICT,statement_date date NOT NULL,statement_balance_minor bigint NOT NULL,ledger_balance_minor bigint NOT NULL,difference_minor bigint NOT NULL,status text NOT NULL DEFAULT 'draft',prepared_by text NOT NULL,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS cheques(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,bank_account_id text NOT NULL REFERENCES bank_accounts(id) ON DELETE RESTRICT,cheque_number text NOT NULL,payee_contact_id text REFERENCES contacts(id) ON DELETE SET NULL,issue_date date NOT NULL,amount_minor bigint NOT NULL,status text NOT NULL DEFAULT 'issued',payment_id text REFERENCES payments(id) ON DELETE SET NULL,cleared_at timestamptz,voided_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS orders(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,type text NOT NULL,number text NOT NULL,contact_id text NOT NULL REFERENCES contacts(id) ON DELETE RESTRICT,issue_date date NOT NULL,expiry_date date,status text NOT NULL DEFAULT 'draft',currency text NOT NULL,subtotal_minor bigint NOT NULL,tax_minor bigint NOT NULL DEFAULT 0,total_minor bigint NOT NULL,converted_document_id text REFERENCES documents(id) ON DELETE SET NULL,custom_fields text NOT NULL DEFAULT '{}',revision integer NOT NULL DEFAULT 1,accepted_at timestamptz,rejected_at timestamptz,fulfilled_quantity_micros bigint NOT NULL DEFAULT 0,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,type,number));
CREATE TABLE IF NOT EXISTS order_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,order_id text NOT NULL REFERENCES orders(id) ON DELETE CASCADE,product_id text REFERENCES products(id) ON DELETE RESTRICT,account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,tax_account_id text REFERENCES accounts(id) ON DELETE RESTRICT,description text NOT NULL,quantity_micros bigint NOT NULL,unit_price_minor bigint NOT NULL,subtotal_minor bigint NOT NULL,tax_minor bigint NOT NULL DEFAULT 0,total_minor bigint NOT NULL,project_id text REFERENCES projects(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS inventory_locations(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,code));
CREATE TABLE IF NOT EXISTS inventory_balances(organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,quantity_micros bigint NOT NULL DEFAULT 0,inventory_value_minor bigint NOT NULL DEFAULT 0,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(organization_id,product_id,location_id));
CREATE TABLE IF NOT EXISTS inventory_movements(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,type text NOT NULL,movement_date date NOT NULL,quantity_delta_micros bigint NOT NULL,unit_cost_minor bigint NOT NULL,value_delta_minor bigint NOT NULL,source_type text NOT NULL,source_id text,transfer_group_id text,journal_entry_id text REFERENCES journal_entries(id) ON DELETE SET NULL,idempotency_key text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE UNIQUE INDEX IF NOT EXISTS inventory_movements_org_idempotency_uq ON inventory_movements(organization_id,idempotency_key);
CREATE TABLE IF NOT EXISTS inventory_reservations(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,source_type text NOT NULL,source_id text NOT NULL,quantity_micros bigint NOT NULL,status text NOT NULL DEFAULT 'active',expires_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS inventory_lots(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,lot_number text NOT NULL,serial_number text,expiry_date date,quantity_micros bigint NOT NULL,unit_cost_minor bigint NOT NULL,received_at timestamptz NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS stock_counts(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,number text NOT NULL,count_date date NOT NULL,status text NOT NULL DEFAULT 'draft',counted_by text NOT NULL,approved_by text,approved_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS stock_count_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,stock_count_id text NOT NULL REFERENCES stock_counts(id) ON DELETE CASCADE,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,expected_quantity_micros bigint NOT NULL,counted_quantity_micros bigint NOT NULL,adjustment_movement_id text REFERENCES inventory_movements(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS delivery_notes(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,order_id text NOT NULL REFERENCES orders(id) ON DELETE RESTRICT,number text NOT NULL,delivery_date date NOT NULL,status text NOT NULL DEFAULT 'draft',notes text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS delivery_note_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,delivery_note_id text NOT NULL REFERENCES delivery_notes(id) ON DELETE CASCADE,order_line_id text NOT NULL REFERENCES order_lines(id) ON DELETE RESTRICT,quantity_micros bigint NOT NULL,location_id text REFERENCES inventory_locations(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS purchase_requisitions(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,number text NOT NULL,requested_by text NOT NULL,required_by date,status text NOT NULL DEFAULT 'draft',description text NOT NULL,estimated_minor bigint NOT NULL DEFAULT 0,approved_by text,approved_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS goods_receipts(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,purchase_order_id text NOT NULL REFERENCES orders(id) ON DELETE RESTRICT,number text NOT NULL,received_date date NOT NULL,location_id text NOT NULL REFERENCES inventory_locations(id) ON DELETE RESTRICT,status text NOT NULL DEFAULT 'draft',supplier_document_number text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS goods_receipt_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,goods_receipt_id text NOT NULL REFERENCES goods_receipts(id) ON DELETE CASCADE,order_line_id text NOT NULL REFERENCES order_lines(id) ON DELETE RESTRICT,product_id text NOT NULL REFERENCES products(id) ON DELETE RESTRICT,quantity_micros bigint NOT NULL,unit_cost_minor bigint NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS recurring_templates(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,type text NOT NULL,name text NOT NULL,template_json text NOT NULL,cadence text NOT NULL,next_run_at timestamptz NOT NULL,auto_post boolean NOT NULL DEFAULT false,active boolean NOT NULL DEFAULT true,last_run_at timestamptz,created_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS approval_policies(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,document_type text NOT NULL,minimum_minor bigint NOT NULL DEFAULT 0,maximum_minor bigint,levels integer NOT NULL DEFAULT 1,approver_roles text NOT NULL,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS document_approval_requests(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,entity_type text NOT NULL,entity_id text NOT NULL,policy_id text REFERENCES approval_policies(id) ON DELETE SET NULL,status text NOT NULL DEFAULT 'pending',current_level integer NOT NULL DEFAULT 1,submitted_by text NOT NULL,submitted_at timestamptz NOT NULL DEFAULT now(),decided_by text,decided_at timestamptz,comments text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS document_attachments(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,entity_type text NOT NULL,entity_id text NOT NULL,filename text NOT NULL,content_type text NOT NULL,object_key text NOT NULL,size_bytes bigint NOT NULL,uploaded_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS document_deliveries(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,entity_type text NOT NULL,entity_id text NOT NULL,channel text NOT NULL,recipient text NOT NULL,status text NOT NULL DEFAULT 'queued',sent_at timestamptz,opened_at timestamptz,failure_reason text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS expense_claims(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,employee_id text NOT NULL REFERENCES employees(id) ON DELETE RESTRICT,number text NOT NULL,claim_date date NOT NULL,currency text NOT NULL,status text NOT NULL DEFAULT 'draft',total_minor bigint NOT NULL DEFAULT 0,submitted_at timestamptz,approved_by text,approved_at timestamptz,reimbursed_payment_id text REFERENCES payments(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS expense_claim_lines(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,claim_id text NOT NULL REFERENCES expense_claims(id) ON DELETE CASCADE,type text NOT NULL,expense_date date NOT NULL,description text NOT NULL,account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,amount_minor bigint NOT NULL,tax_minor bigint NOT NULL DEFAULT 0,mileage_micros bigint,per_diem_days integer,project_id text REFERENCES projects(id) ON DELETE SET NULL,receipt_attachment_id text REFERENCES document_attachments(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS corporate_cards(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,employee_id text REFERENCES employees(id) ON DELETE SET NULL,name text NOT NULL,last_four text NOT NULL,ledger_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS receivable_actions(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,document_id text NOT NULL REFERENCES documents(id) ON DELETE CASCADE,type text NOT NULL,action_date date NOT NULL,amount_minor bigint NOT NULL DEFAULT 0,status text NOT NULL DEFAULT 'completed',notes text,journal_entry_id text REFERENCES journal_entries(id) ON DELETE SET NULL,created_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS payment_plans(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,document_id text NOT NULL REFERENCES documents(id) ON DELETE CASCADE,status text NOT NULL DEFAULT 'active',created_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS payment_plan_installments(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,payment_plan_id text NOT NULL REFERENCES payment_plans(id) ON DELETE CASCADE,due_date date NOT NULL,amount_minor bigint NOT NULL,paid_minor bigint NOT NULL DEFAULT 0,status text NOT NULL DEFAULT 'pending',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS close_checklist_items(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,period_id text NOT NULL REFERENCES fiscal_periods(id) ON DELETE CASCADE,code text NOT NULL,name text NOT NULL,required boolean NOT NULL DEFAULT true,status text NOT NULL DEFAULT 'pending',evidence text,completed_by text,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(organization_id,period_id,code));
CREATE TABLE IF NOT EXISTS adjustment_schedules(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,type text NOT NULL,name text NOT NULL,start_date date NOT NULL,end_date date,frequency text NOT NULL,amount_minor bigint NOT NULL,debit_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,credit_account_id text NOT NULL REFERENCES accounts(id) ON DELETE RESTRICT,next_posting_date date,status text NOT NULL DEFAULT 'active',metadata text NOT NULL DEFAULT '{}',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS close_signoffs(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,period_id text NOT NULL REFERENCES fiscal_periods(id) ON DELETE CASCADE,stage text NOT NULL,status text NOT NULL DEFAULT 'pending',requested_by text NOT NULL,approved_by text,comment text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS compliance_approvals(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,entity_type text NOT NULL,entity_id text NOT NULL,action text NOT NULL,status text NOT NULL DEFAULT 'pending',requested_by text NOT NULL,reviewed_by text,reason text,created_at timestamptz NOT NULL DEFAULT now(),reviewed_at timestamptz);
CREATE TABLE IF NOT EXISTS segregation_rules(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,name text NOT NULL,entity_type text NOT NULL,action text NOT NULL,requester_roles text NOT NULL DEFAULT '[]',approver_roles text NOT NULL DEFAULT '[]',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS attachments(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,entity_type text NOT NULL,entity_id text NOT NULL,file_name text NOT NULL,content_type text NOT NULL,object_key text NOT NULL,sha256 text NOT NULL,retention_until date,uploaded_by text NOT NULL,created_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS report_layouts(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,name text NOT NULL,report_type text NOT NULL,groups_json text NOT NULL DEFAULT '[]',columns_json text NOT NULL DEFAULT '[]',branding_json text NOT NULL DEFAULT '{}',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS report_annotations(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,report_type text NOT NULL,period_key text NOT NULL,text text NOT NULL,status text NOT NULL DEFAULT 'draft',created_by text NOT NULL,signed_off_by text,signed_off_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS report_jobs(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,requested_by text NOT NULL,report_type text NOT NULL,format text NOT NULL,filters text NOT NULL DEFAULT '{}',status text NOT NULL DEFAULT 'queued',object_key text,error text,completed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS report_schedules(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,name text NOT NULL,report_type text NOT NULL,format text NOT NULL,filters text NOT NULL DEFAULT '{}',cron text NOT NULL,recipients text NOT NULL DEFAULT '[]',active boolean NOT NULL DEFAULT true,next_run_at timestamptz,last_run_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS time_entries(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,user_id text REFERENCES users(id) ON DELETE SET NULL,project_id text NOT NULL REFERENCES projects(id) ON DELETE CASCADE,entry_date date NOT NULL,minutes integer NOT NULL,description text,billable boolean NOT NULL DEFAULT false,billed boolean NOT NULL DEFAULT false,cost_rate_minor bigint NOT NULL DEFAULT 0,billing_rate_minor bigint NOT NULL DEFAULT 0,status text NOT NULL DEFAULT 'draft',created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS ledgerly_mobile_document_intents(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,device_id text,intent_type text NOT NULL,payload_json text NOT NULL,status text NOT NULL DEFAULT 'pending',server_document_id text REFERENCES documents(id) ON DELETE SET NULL,error_code text,error_message text,attempts integer NOT NULL DEFAULT 0,next_attempt_at timestamptz,created_by text NOT NULL REFERENCES users(id) ON DELETE RESTRICT,client_created_at timestamptz NOT NULL,applied_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE IF NOT EXISTS ledgerly_mobile_journal_intents(id text PRIMARY KEY,organization_id text NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,device_id text,payload_json text NOT NULL,status text NOT NULL DEFAULT 'pending',server_journal_id text REFERENCES journal_entries(id) ON DELETE SET NULL,error_code text,error_message text,attempts integer NOT NULL DEFAULT 0,next_attempt_at timestamptz,created_by text NOT NULL REFERENCES users(id) ON DELETE RESTRICT,client_created_at timestamptz NOT NULL,applied_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());
CREATE INDEX IF NOT EXISTS inventory_movements_product_date_idx ON inventory_movements(organization_id,product_id,movement_date);CREATE INDEX IF NOT EXISTS report_schedules_due_idx ON report_schedules(active,next_run_at);CREATE INDEX IF NOT EXISTS ledgerly_mobile_document_intents_pending_idx ON ledgerly_mobile_document_intents(status,next_attempt_at,created_at);CREATE INDEX IF NOT EXISTS ledgerly_mobile_journal_intents_pending_idx ON ledgerly_mobile_journal_intents(status,next_attempt_at,created_at);
`);}
