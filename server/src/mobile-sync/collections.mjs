import { readdir } from "node:fs/promises";
const DIRECTORY = new URL("./collections/", import.meta.url); const SUFFIX = ".collection.mjs";
function normalize(definition,sourceFile){if(!definition||typeof definition!=="object")throw new TypeError(`Sync collection ${sourceFile} must export an object`);const moduleKey=String(definition.moduleKey??"").trim(),collectionKey=String(definition.collectionKey??"").trim();if(!moduleKey||!collectionKey)throw new TypeError(`Sync collection ${sourceFile} requires moduleKey and collectionKey`);if(typeof definition.canRead!=="function")throw new TypeError(`Sync collection ${moduleKey}:${collectionKey} requires canRead()`);if(definition.apply!=null&&typeof definition.apply!=="function")throw new TypeError(`Sync collection ${moduleKey}:${collectionKey} apply must be a function`);return Object.freeze({...definition,moduleKey,collectionKey,sourceFile});}
export async function loadMobileSyncCollections(context={}){let entries;try{entries=await readdir(DIRECTORY);}catch(error){if(error?.code==='ENOENT')return[];throw error;}const definitions=[],keys=new Set();for(const entry of entries.filter(n=>n.endsWith(SUFFIX)).sort()){const module=await import(new URL(`./collections/${entry}`,import.meta.url));const raw=typeof module.createCollection==='function'?await module.createCollection(context):(module.default??module.collection);const definition=normalize(raw,entry);const k=`${definition.moduleKey}:${definition.collectionKey}`;if(keys.has(k))throw new Error(`Duplicate mobile sync collection: ${k}`);keys.add(k);definitions.push(definition);}return definitions;}
