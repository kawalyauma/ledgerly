import {PostgresPlatformConfigRepository} from '../platform-config/repository.mjs';
import {PlatformConfigService} from '../platform-config/service.mjs';
export default{name:'platform-config',required:false,configure(env){return{enabled:String(env.SELFHOST_PLATFORM_CONFIG_ENABLED??'').toLowerCase()==='true'};},enabled(c){return c.enabled===true;},async create({services}){const repository=new PostgresPlatformConfigRepository({database:services.database});await repository.ensureSchema();const service=new PlatformConfigService({repository,audit:services.audit});return{value:Object.freeze({repository,service}),readiness:async()=>{const r=await services.database.query(`SELECT to_regclass('public.platform_module_overrides') IS NOT NULL AS modules,to_regclass('public.platform_feature_overrides') IS NOT NULL AS features`);return{ok:r.rows[0]?.modules===true&&r.rows[0]?.features===true,provider:'postgresql-platform-config'};},describe(){return{provider:'postgresql-platform-config',canonicalCatalog:'modules/catalog.generated.ts',tenantOverridesOnly:true,cloudflareFallbackPreserved:true};}};}};
