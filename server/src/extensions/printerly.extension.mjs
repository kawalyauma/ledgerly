import {PrinterlyRuntimeService} from '../printerly/runtime-service.mjs';
import {PrinterlyCostingService} from '../printerly/costing-service.mjs';
import {PrinterlyNodeService} from '../printerly/node-service.mjs';
import {PrinterlyScannerService} from '../printerly/scanner-service.mjs';
import {PrinterlyProcurementService} from '../printerly/procurement-service.mjs';
import {PrinterlyDispatchWorker} from '../printerly/dispatch-worker.mjs';
import {PrinterlyMaintenanceService} from '../printerly/maintenance-service.mjs';
import {PrinterlyBatchService} from '../printerly/batch-service.mjs';
import {PrinterlyJobService} from '../printerly/job-service.mjs';
import {PrinterlyApprovalService} from '../printerly/approval-service.mjs';
import {PrinterlyDocumentService} from '../printerly/document-service.mjs';
import {PrinterlyQueueWorker} from '../printerly/queue-worker.mjs';
import {PrinterlyScheduleRegistry} from '../printerly/schedule-registry.mjs';
const modes=new Set(['cloudflare','shadow','node']);
function mode(value,name='LEDGERLY_PRINTERLY_CUTOVER'){const v=String(value||'cloudflare').toLowerCase();if(!modes.has(v))throw new Error(`${name} must be cloudflare, shadow, or node`);return v;}
export default{
 name:'printerly',required:false,
 configure(env){const cutover=mode(env.LEDGERLY_PRINTERLY_CUTOVER),nodeCutover=mode(env.LEDGERLY_PRINTERLY_NODE_CUTOVER||cutover,'LEDGERLY_PRINTERLY_NODE_CUTOVER'),jobCutover=mode(env.LEDGERLY_PRINTERLY_JOB_CUTOVER,'LEDGERLY_PRINTERLY_JOB_CUTOVER'),scannerCutover=mode(env.LEDGERLY_PRINTERLY_SCANNER_CUTOVER||cutover,'LEDGERLY_PRINTERLY_SCANNER_CUTOVER'),procurementCutover=mode(env.LEDGERLY_PRINTERLY_PROCUREMENT_CUTOVER||cutover,'LEDGERLY_PRINTERLY_PROCUREMENT_CUTOVER');if(jobCutover==='node'&&nodeCutover!=='node')throw new Error('LEDGERLY_PRINTERLY_JOB_CUTOVER=node requires LEDGERLY_PRINTERLY_NODE_CUTOVER=node so accepted jobs can be claimed by self-hosted Printerly Nodes');return{cutover,nodeCutover,jobCutover,scannerCutover,procurementCutover,outboxPollMs:Math.max(500,Number(env.LEDGERLY_PRINTERLY_OUTBOX_POLL_MS)||2000),workerPollMs:Math.max(250,Number(env.LEDGERLY_PRINTERLY_WORKER_POLL_MS)||1000)};},
 enabled(){return true;},
 async create({services,createQueue,extensionConfig}){const queue=createQueue({name:'printerly',maxAttempts:8}),runtime=new PrinterlyRuntimeService({database:services.database}),costing=new PrinterlyCostingService({database:services.database}),node=new PrinterlyNodeService({database:services.database,storage:services.storage,runtime,costing}),scanner=new PrinterlyScannerService({database:services.database,storage:services.storage}),procurement=new PrinterlyProcurementService({database:services.database}),jobs=new PrinterlyJobService({database:services.database,runtime}),approvals=new PrinterlyApprovalService({database:services.database}),documents=new PrinterlyDocumentService({database:services.database,storage:services.storage}),outbox=new PrinterlyDispatchWorker({database:services.database,queue,maxAttempts:8}),maintenance=new PrinterlyMaintenanceService({database:services.database,storage:services.storage,runtime}),batches=new PrinterlyBatchService({database:services.database}),schedules=new PrinterlyScheduleRegistry({database:services.database,scheduler:services.scheduler});const runtimeEnabled=[extensionConfig.cutover,extensionConfig.nodeCutover,extensionConfig.jobCutover,extensionConfig.scannerCutover,extensionConfig.procurementCutover].some(v=>v!=='cloudflare'),organization=job=>{const v=String(job.organizationId||job.payload?.organizationId||'').trim();if(!v)throw new Error('Printerly job is missing organizationId');return v;},kindEnabled=kind=>kind==='printerly.procurement'?extensionConfig.procurementCutover==='node':extensionConfig.cutover==='node',run=method=>async job=>kindEnabled(job.kind)?maintenance[method](organization(job)):{skipped:true,reason:'cloudflare-authoritative'},runBatch=async job=>{if(!kindEnabled(job.kind))return{skipped:true,reason:'cloudflare-authoritative'};const org=organization(job),result=await batches.dispatch(org);await maintenance.batchStatus(org);return result;},worker=new PrinterlyQueueWorker({queue,handlers:{'printerly.health':run('health'),'printerly.batch-dispatch':runBatch,'printerly.batch-status':run('batchStatus'),'printerly.retention':run('retention'),'printerly.consumables':run('consumables'),'printerly.service-sla':run('serviceSla'),'printerly.routing':run('routing'),'printerly.release-cleanup':run('releaseCleanup'),'printerly.procurement':run('procurement')}});let outboxTimer=null,workerTimer=null,outboxBusy=false,workerBusy=false,scheduleState={enabled:false};const drainOutbox=async()=>{if(outboxBusy||!runtimeEnabled)return;outboxBusy=true;try{await outbox.drain({limit:100});}catch(error){console.error(JSON.stringify({level:'error',component:'printerly-outbox',message:error instanceof Error?error.message:String(error)}));}finally{outboxBusy=false;}},drainQueue=async()=>{if(workerBusy||!runtimeEnabled)return;workerBusy=true;try{await worker.runOnce({limit:50});}catch(error){console.error(JSON.stringify({level:'error',component:'printerly-worker',message:error instanceof Error?error.message:String(error)}));}finally{workerBusy=false;}};const enabledKinds=[];if(extensionConfig.cutover==='node')enabledKinds.push('printerly.health','printerly.batch-dispatch','printerly.batch-status','printerly.retention','printerly.consumables','printerly.service-sla','printerly.routing','printerly.release-cleanup');if(extensionConfig.procurementCutover==='node')enabledKinds.push('printerly.procurement');try{scheduleState=await schedules.reconcile({enabled:enabledKinds.length>0,enabledKinds});}catch(error){scheduleState={enabled:false,error:error instanceof Error?error.message:String(error)};console.error(JSON.stringify({level:'error',component:'printerly-schedules',message:scheduleState.error}));}if(runtimeEnabled){outboxTimer=setInterval(()=>void drainOutbox(),extensionConfig.outboxPollMs);outboxTimer.unref?.();workerTimer=setInterval(()=>void drainQueue(),extensionConfig.workerPollMs);workerTimer.unref?.();void drainOutbox();void drainQueue();}return{value:Object.freeze({cutover:extensionConfig.cutover,nodeCutover:extensionConfig.nodeCutover,jobCutover:extensionConfig.jobCutover,scannerCutover:extensionConfig.scannerCutover,procurementCutover:extensionConfig.procurementCutover,queue,runtime,costing,node,scanner,procurement,jobs,approvals,documents,outbox,maintenance,batches,worker,schedules}),schedulerQueues:Object.fromEntries(['printerly.health','printerly.batch-dispatch','printerly.batch-status','printerly.retention','printerly.consumables','printerly.service-sla','printerly.routing','printerly.release-cleanup','printerly.procurement'].map(kind=>[kind,queue])),async readiness(){if(!runtimeEnabled)return{ok:true,cutover:'cloudflare',authoritative:false,schedules:scheduleState};const[deadOutbox,deadQueue]=await Promise.all([services.database.query(`SELECT COUNT(*)::int count FROM prn_dispatch_outbox WHERE status='dead'`),queue.deadLetterSize()]);return{ok:Number(deadOutbox.rows[0]?.count||0)===0&&Number(deadQueue||0)===0&&!scheduleState.error,cutover:extensionConfig.cutover,nodeCutover:extensionConfig.nodeCutover,jobCutover:extensionConfig.jobCutover,scannerCutover:extensionConfig.scannerCutover,procurementCutover:extensionConfig.procurementCutover,deadOutbox:Number(deadOutbox.rows[0]?.count||0),deadQueue:Number(deadQueue||0),schedules:scheduleState};},describe(){return{cutover:extensionConfig.cutover,nodeCutover:extensionConfig.nodeCutover,jobCutover:extensionConfig.jobCutover,scannerCutover:extensionConfig.scannerCutover,procurementCutover:extensionConfig.procurementCutover,outbox:'postgres-to-redis',worker:runtimeEnabled?'redis-consumer':'disabled',schedules:scheduleState};},async close(){if(outboxTimer)clearInterval(outboxTimer);if(workerTimer)clearInterval(workerTimer);}};}
};
