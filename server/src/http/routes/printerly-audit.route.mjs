import {authenticatePrinterly,data,httpifyPrinterlyError} from '../printerly-http.mjs';const P='/api/v1/printerly/audit';export default{name:'printerly-audit',prefix:P,business:true,priority:87,enabled(c){return c.extensions?.printerly?.auditCutover==='node';},async handle({request,url,runtime}){try{const p=await authenticatePrinterly(runtime,request),s=runtime.extensions.printerly.operations;if(request.method==='GET'&&url.pathname===`${P}/users`)return data(await s.auditUsers(p.organizationId));if(request.method==='GET'&&url.pathname===P)return data(await s.auditRecords(p.organizationId,Object.fromEntries(url.searchParams.entries())));const e=new Error('Printerly audit route not found');e.code='PRINTERLY_ROUTE_NOT_FOUND';e.status=404;throw e;}catch(e){throw httpifyPrinterlyError(e);}}};
