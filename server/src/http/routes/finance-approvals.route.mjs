import {authenticateFinance,mapFinanceError,ok,readJson} from '../../finance/http.mjs';
const PREFIX='/api/v1/operations/approvals';
const parts=url=>url.pathname.slice(PREFIX.length).split('/').filter(Boolean);
export default{name:'finance-approvals',prefix:PREFIX,business:true,priority:98,enabled:c=>c.extensions?.['finance-api']?.approvalsCutover==='node',matches({request,url}){const p=parts(url);return request.method==='POST'&&p.length===2&&['approve','reject','revise'].includes(p[1]);},async handle({request,url,runtime,requestId}){try{const api=runtime.extensions?.['finance-api']?.approvals;if(!api)throw Object.assign(new Error('Finance approvals runtime unavailable'),{status:503,code:'FINANCE_SELFHOST_NOT_READY'});const p=parts(url),principal=await authenticateFinance(runtime,request,'documents:write');return ok({data:await api.decide({principal,approvalId:p[0],decision:p[1],body:await readJson(request,{optional:true}),requestId})});}catch(error){throw mapFinanceError(error)}}};
