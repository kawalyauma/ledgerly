import {authenticateFinance,mapFinanceError,ok,readJson} from '../../finance/http.mjs';
const PREFIX='/api/v1/operations/approval-policies';
export default{name:'finance-approval-policies',prefix:PREFIX,business:true,priority:98,enabled:c=>c.extensions?.['finance-api']?.approvalsCutover==='node',matches({request,url}){return request.method==='POST'&&url.pathname===PREFIX;},async handle({request,runtime,requestId}){try{const api=runtime.extensions?.['finance-api']?.approvals;if(!api)throw Object.assign(new Error('Finance approvals runtime unavailable'),{status:503,code:'FINANCE_SELFHOST_NOT_READY'});const principal=await authenticateFinance(runtime,request,'admin:write');return ok({data:await api.createPolicy({principal,body:await readJson(request),requestId})},201);}catch(error){throw mapFinanceError(error)}}};
