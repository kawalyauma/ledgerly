import {authenticatePrinterly,data,httpifyPrinterlyError} from '../printerly-http.mjs';const P='/api/v1/printerly/reports';export default{name:'printerly-reports',prefix:P,business:true,priority:87,enabled(c){return c.extensions?.printerly?.coreCutover==='node';},async handle({request,url,runtime}){try{const p=await authenticatePrinterly(runtime,request),s=runtime.extensions.printerly.management;if(request.method==='GET'&&url.pathname===`${P}/usage`)return data(await s.usageReport(p.organizationId,url.searchParams.get('from'),url.searchParams.get('to')));if(request.method==='GET'&&url.pathname===`${P}/usage.csv`){const r=await s.usageCsv(p.organizationId,url.searchParams.get('from'),url.searchParams.get('to'));return{status:200,rawBody:Buffer.from(r.csv),headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="printerly-usage-${r.from}-to-${r.to}.csv"`}};}const e=new Error('Printerly report route not found');e.code='PRINTERLY_ROUTE_NOT_FOUND';e.status=404;throw e;}catch(e){throw httpifyPrinterlyError(e);}}};
