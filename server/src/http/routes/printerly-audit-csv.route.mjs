import {authenticatePrinterly,httpifyPrinterlyError} from '../printerly-http.mjs';const P='/api/v1/printerly/audit.csv';export default{name:'printerly-audit-csv',prefix:P,business:true,priority:88,enabled(c){return c.extensions?.printerly?.auditCutover==='node';},async handle({request,url,runtime}){try{if(request.method==='GET'&&url.pathname===P){const p=await authenticatePrinterly(runtime,request),r=await runtime.extensions.printerly.operations.auditCsv(p.organizationId,Object.fromEntries(url.searchParams.entries()));return{status:200,rawBody:Buffer.from(r.csv),headers:{'content-type':'text/csv; charset=utf-8','content-disposition':`attachment; filename="printerly-audit-${r.from}-to-${r.to}.csv"`}};}const e=new Error('Printerly audit CSV route not found');e.code='PRINTERLY_ROUTE_NOT_FOUND';e.status=404;throw e;}catch(e){throw httpifyPrinterlyError(e);}}};
