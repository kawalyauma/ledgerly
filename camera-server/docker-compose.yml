services:
  mediamtx:
    image: bluenviron/mediamtx:1.21.0
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./mediamtx.yml:/mediamtx.yml:ro
      - ./camera-storage:/recordings
    command: ["/mediamtx.yml"]
