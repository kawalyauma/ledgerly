import{AppError}from'../../../src/lib/errors';
export async function processPendingHrMobileIntents(db:D1Database,options:{organizationId?:string;limit?:number}={}){const org=options.organizationId,limit=Math.min(Math.max(options.limit||25,1),100);await db.prepare(`UPDATE hr_mobile_leave_intents SET status='pending',error_message=COALESCE(error_message,'Recovered interrupted conversion'),updated_at=CURRENT_TIMESTAMP WHERE status='processing' AND updated_at<datetime('now','-10 minutes')${org?' AND organization_id=?':''}`).bind(...(org?[org]:[])).run();const l=await db.prepare(`SELECT * FROM hr_mobile_leave_intents WHERE status='pending'${org?' AND organization_id=?':''} ORDER BY created_at LIMIT ?`).bind(...(org?[org,limit]:[limit])).all<any>();for(const r of l.results){await db.prepare("UPDATE hr_mobile_leave_intents SET status='processing',updated_at=CURRENT_TIMESTAMP WHERE id=? AND status='pending'").bind(r.id).run();try{const exists=await db.prepare('SELECT id FROM hr_leave_requests WHERE id=? AND organization_id=?').bind(r.id,r.organization_id).first();if(!exists)await db.prepare(`INSERT INTO hr_leave_requests(id,organization_id,employee_id,leave_type_id,starts_on,ends_on,days_micros,reason,status,requested_by,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?, 'pending',?,?,?)`).bind(r.id,r.organization_id,r.employee_id,r.leave_type_id,r.starts_on,r.ends_on,r.days_micros,r.reason,r.requested_by,r.client_created_at,r.client_created_at).run();await db.prepare("UPDATE hr_mobile_leave_intents SET status='applied',server_leave_request_id=id,applied_at=COALESCE(applied_at,CURRENT_TIMESTAMP),updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(r.id).run()}catch(e){const msg=(e instanceof Error?e.message:String(e)).slice(0,1000);await db.prepare("UPDATE hr_mobile_leave_intents SET status='rejected',error_message=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(msg,r.id).run()}}
const c=await db.prepare(`SELECT * FROM hr_mobile_onboarding_intents WHERE status='pending'${org?' AND organization_id=?':''} ORDER BY created_at LIMIT ?`).bind(...(org?[org,limit]:[limit])).all<any>();for(const r of c.results){try{const x=await db.prepare("UPDATE hr_onboarding_tasks SET status='completed',completed_at=COALESCE(completed_at,?),updated_at=CURRENT_TIMESTAMP WHERE id=? AND organization_id=? AND status<>'completed'").bind(r.client_completed_at,r.task_id,r.organization_id).run();if(!x.meta.changes){const exists=await db.prepare('SELECT id FROM hr_onboarding_tasks WHERE id=? AND organization_id=?').bind(r.task_id,r.organization_id).first();if(!exists)throw new AppError(404,'ONBOARDING_TASK_NOT_FOUND','Onboarding task not found')}await db.prepare("UPDATE hr_mobile_onboarding_intents SET status='applied',applied_at=COALESCE(applied_at,CURRENT_TIMESTAMP),updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(r.id).run()}catch(e){await db.prepare("UPDATE hr_mobile_onboarding_intents SET status='rejected',error_message=?,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind((e instanceof Error?e.message:String(e)).slice(0,1000),r.id).run()}}return{leaveProcessed:l.results.length,onboardingProcessed:c.results.length}}
