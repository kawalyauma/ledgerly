import{createPayment}from'../../../src/services/payments';
export async function processPendingPaymentMobileIntents(db:D1Database,options:{organizationId?:string;limit?:number}={}){const org=options.organizationId,limit=Math.min(Math.max(options.limit||25,1),100);await db.prepare(`UPDATE pay_mobile_payment_intents SET status='pending',error_message=COALESCE(error_message,'Recovered interrupted conversion'),updated_at=CURRENT_TIMESTAMP WHERE status='processing' AND updated_at<datetime('now','-10 minutes')${org?' AND organization_id=?':''}`).bind(...(org?[org]:[])).run();const q=await db.prepare(`SELECT * FROM pay_mobile_payment_intents WHERE status='pending'${org?' AND organization_id=?':''} ORDER BY created_at LIMIT ?`).bind(...(org?[org,limit]:[limit])).all<any>();for(const r of q.results){const claimed=await db.prepare("UPDATE pay_mobile_payment_intents SET status='processing',attempts=attempts+1,updated_at=CURRENT_TIMESTAMP WHERE id=? AND organization_id=? AND status='pending'").bind(r.id,r.organization_id).run();if(!claimed.meta.changes)continue;try{const payload=JSON.parse(r.payload_json),created=await createPayment(db,r.organization_id,r.created_by,payload,`mobile-payment:${r.id}`);await db.prepare("UPDATE pay_mobile_payment_intents SET status='applied',server_payment_id=?,error_message=NULL,applied_at=COALESCE(applied_at,CURRENT_TIMESTAMP),updated_at=CURRENT_TIMESTAMP WHERE id=? AND organization_id=?").bind((created as any).id,r.id,r.organization_id).run()}catch(e){await db.prepare("UPDATE pay_mobile_payment_intents SET status='rejected',error_message=?,updated_at=CURRENT_TIMESTAMP WHERE id=? AND organization_id=?").bind((e instanceof Error?e.message:String(e)).slice(0,1000),r.id,r.organization_id).run()}}return{processed:q.results.length}}
